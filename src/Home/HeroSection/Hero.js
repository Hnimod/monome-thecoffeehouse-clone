import React from 'react';
import { Section, Container } from './HeroElements';

const Hero = () => {
  return (
    <Section>
      <Container></Container>
    </Section>
  );
};

export default Hero;
